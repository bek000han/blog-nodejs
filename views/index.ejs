<div class="author">
    <h1 class="author__heading">Welcome to my blog!</h1>
    <p class="author__body">random thoughts of a random guy</p>
</div>
<div class="image-container">
    <img src="/img/hikingpicme1.jpg" alt="me in grizedale" class="hero-image">
</div>
<section class="articles">
    <h2 class="articles__heading">Latest Blogs</h2>
    <ul class="article-ul">
        <% data.forEach(post => { %>
            <li>
                <a href="/post/<%= post._id %>">
                    <span><%= post.title %></span>
                    <span class="article-list__date"><%= post.createdAt.toDateString() %></span>
                </a>
            </li>
        <% }) %>
    </ul>
    <% if (nextPage !== null) { %>
        <a href="/?page=<%= nextPage %>" class="pagination">&lt; View older posts</a>
    <% } %>
</section>